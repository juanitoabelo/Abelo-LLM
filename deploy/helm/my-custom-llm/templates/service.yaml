apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-custom-llm.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8000
  selector:
    app: {{ include "my-custom-llm.name" . }}
