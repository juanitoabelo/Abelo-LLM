[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_custom_llm"
version = "0.1.0"
description = "Tiny from-scratch autoregressive language model using PyTorch"
requires-python = ">=3.8"

[tool.poetry]
# optional: kept minimal
